server:
  address: ":8085"

stores:
  test:
    type: consul
    config:
      prefix: bundles/test
      consul:
        address: http://consul1:8500

webhooks:
  test:
    type: gogs
    config:
      secret: sauce
      events:
        - push

bundles:
  test:
    store: test
    webhook: test
    